class people::russellt::git {
  git::config::global {
    'color.branch'      : value => 'auto';
    'color.diff'        : value => 'auto';
    'color.interactive' : value => 'auto';
    'color.status'      : value => 'auto';

    'github.user'   : value => 'RussellT';

    'user.name'     : value => 'Alex Tareshawty';
    'user.email'    : value => 'tareshawty.3@osu.edu';
    'user.username' : value => 'RussellT';
  }
}
